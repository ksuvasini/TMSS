@model IEnumerable<TMSS.Domain.Entities.Clinic>


@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h4>Clinic Details</h4>

<p>
    <a asp-action="Create">Create New</a>
</p>
<style type="text/css">
    form {
        float: right;
    }

</style>


<form action="/Clinic/Index" method="get">

    <div class="input-group md-form form-sm form-2 p1-0">
        <input name="searchstring" type="text" style="width:400px;" placeholder="Search" class="form-control" />
        <div class="input-group-btn">
            <button class="btn btn-primary" type="submit" onclick="fnClinicDetails()">
                <i class="fas fa-search" aria-hidden="true"></i>
                @*                 <i class="fas fa-search text-grey" aria-hidden="true"></i>
                *@
            </button>
        </div>
    </div>
</form>

@* <div class="input-group">
    <input type="text" name="clinicname" class="form-control" placeholder="Search">
    <div class="input-group-btn">
        <button class="btn btn-default" type="submit" onclick="fnClinicDetails()">
            <i class="glyphicon glyphicon-search"></i>
        </button>
    </div>
</div>

 *@
<br />
<br />
<!DOCTYPE html>
<html lang="en">
<head>
    <title>Table In HTML</title>
    <style>
        table, th, td {
            border: 1px solid black;
            border-collapse: collapse;
        }
    </style>


</head>
<body>
    <div>
        <table style="width:100%;">
            <thead>
                <tr>
                    <th>
                        @*                 @Html.DisplayNameFor(model => model.ClinicName)
                        *@
                        Clinic Name
                    </th>
                    <th>
                        @*                 @Html.DisplayNameFor(model => model.ClinicLocation)
                        *@
                        Clinic Location
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.ClinicName)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.ClinicLocation)
                        </td>
                        <td>
                            <a asp-action="Edit" asp-route-id="@item.ClinicId">Edit</a> |
                            <a asp-action="Details" asp-route-id="@item.ClinicId">Details</a> |
                        </td>
                    </tr>
                }

            </tbody>
        </table>


        @section scripts{
            <script>
                $(function () {
                    alert(hi)
                    $("btnclinicsearch").click(function () {
                        $("Form").submit();
                    })

                });
            </script>

            <script>
                function fnClinicDetails() {
                    alert(hi)

                    $("Form").submit();

                }
            </script>
        }

    </div>
</body>
</html>



